/*

    thread support library
    **********************
Defined in header <future>

    std::launch ( Oxford dictionary: to start an activity ... )
    ***********

Specifies the launch policy of a task executed by std::async function.
std::launch is an enumeration as BitMaskType( bit mask type ).

std::launch::async      a new thread is launched to execute the task asynchronously
std::launch::deferred   the task is executed on the calling thread the first time its result to requested( lazy evaluation ).

*/
/// std::launch

#include <iostream>
#include <future>
#include <algorithm>
#include <vector>
#include <numeric>

}


int main(){

    // has no example, see the previous file ...

}

/* output for me:



*/








