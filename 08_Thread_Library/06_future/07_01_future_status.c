/*

    thread support library
    **********************
Defined in header <future>

    std::future_status
    ******************

Specifies stats of a future as returned by wait_for and wait_until
functions of std::future and std::shared_future.

std::future_status::deferred    the shared state contains a deferred function, so the result will be computed one when explicitly requested
std::future_status::ready       the shared state is ready
std::future_status::timeout     the shared state did not become ready before specified timeout duration has passed

*/
/// std::launch

#include <iostream>
#include <future>
#include <algorithm>
#include <vector>
#include <numeric>

}


int main(){

    // has no example, see the previous file ...

}

/* output for me:



*/








