/**
std::basic_ifstream
**/

/// 07_std::basic_ifstream::close
/**
void close();
Close the associated file.
Effectively call rdbuf()->close(). if an error occurs during operation,
setstate(failbit) is called.
setstate(failbit) is called.

NOTE : This function is called by the destructor of basic_ifstream when
       the stream object goes out of scope and is not usually invoked directly.

**/
#include <iostream>
#include <string>
#include <fstream>

int main()
{
    {
        /// owner
    }
    {
        /// en.reference.com
    }
    {
        /// cplusplus.com
    }

    {
        /// msdn.microsoft.com
    }

}

