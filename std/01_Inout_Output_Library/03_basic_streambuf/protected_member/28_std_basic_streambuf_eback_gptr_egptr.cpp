/**
std::basic_streambuf
**/


/// 28_std::basic_streambuf::eback and pgtr and egptr
/**
char_type* eback() const; /// beginning
char_type* gptr() const;  /// current
char_type* egptr() const; /// end

Returns pointers defining the get area.
1)  Returns the pointer to the beginning of the get area.
2)  Returns the pointer to the current character( get pointer) in the get area.
3)  Returns the pointer to the end of the get area.
**/
#include <iostream>
#include <sstream>
#include <fstream>

int main()
{
    {
        /// owner
    }

    {
        /// en.reference.com
        /// no sample
    }
    {
        /// cplusplus.com
    }

    {
        /// msdn.microsoft.com
    }

}













