/**
std::basic_fstream
**/

/// 07_std::basic_fstream::swap ( algorithm )
/**

template<class CharT, class Traits>
void swap ( basic_fstream<CharT, Traits>& lhs, basic_fstream<CharT, Traits>& rhs);

Specialize the std::swap algorithm for basic_fstream. Exchanges the state of lhs with that
of rhs. Effectively call lhs.swap(rsh).

**/
#include <iostream>
#include <string>
#include <fstream>

int main()
{
    {
        /// owner
    }
    {
        /// en.reference.com
    }

    {
        /// cplusplus.com
    }

    {
        /// msdn.microsoft.com
    }

}

