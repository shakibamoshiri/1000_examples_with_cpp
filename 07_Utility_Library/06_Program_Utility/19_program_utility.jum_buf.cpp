/*
            Program support Utility
            ***********************

Types:

    > jmp_buf           : execution context type

*/
/// std::jmp_buf
//  typedef /* unspecified */ jmp_buf
//  The std::jmp_buf type is an array of suitable for storing information
//  to restore a calling environment. The stored information is sufficient
//  to restore execution at he correct block of the program and invocation
//  of that block.
//  The state of floating-point status flags, or open file, or any other date
//  in not stored in an object of type jmp_buf.
#include <iostream>
#include <csetjmp>


int main (){ /* has no example */ }

/* output:



*/
