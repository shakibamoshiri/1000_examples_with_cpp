/**
std::basic_stringbuf
**/
/// non-function member
/// 11_std::swap(std::basic_stringbuf)
/**
template < class CharT, class Traits, class Alloc>
void swap ( std::basic_stringbuf<CharT, Traits,Alloc>& lhs,
           std::basic_stringbuf<CharT, Traits, Alloc>& rhs )

Override the swap algorithm for std::basic_stringbuf.
Exchange the state of lhs with that of rhs. Effectively
call lhs.swap(rsh).

**/
#include <iostream>
#include <sstream>
#include <fstream>
int main()
{
    {
        /// owner
    }
    {
        /// en.reference.com
    }

    {
        /// cplusplus.com
    }

    {
        /// msdn.microsoft.com
    }

}
