/// ios_base

#include <iostream>
#include <fstream>



using std::cout;
using std::cin;
using std::endl;
using std::cerr;

/// std::ios_base::openmode
/*

static constexpr oprnmode app
static constexpr oprnmode binary
static constexpr oprnmode in
static constexpr oprnmode out
static constexpr oprnmode trunc
static constexpr oprnmode ate

app    : seek to the end of stream before each write
binary : open in binary mode
in     : open in reading
out    : open for writing
trunc  : discard the contents of the stream when opening
ate    : seek to the end of stream immediately after open


*/

int main() {
    {
        /// mine : did not know
        std::ios_base::app
        std::ios_base::binary
        std::ios_base::in
        std::ios_base::out
        std::ios_base::trunc
        std::ios_base::ate


    }
    {
        /// the site no sample
        /// no sample
    }
}





















