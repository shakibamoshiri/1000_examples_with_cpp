/**
std::basic_filebuf
**/

/// non-member :
/// 16_std::basic_filebuf::swap
/**

non-member:
template<class CharT, Traits>
    void swap ( std::basic_filebuf<CharT, Traits>& lhs,
                std::basic_filebuf<CharT, Traits>& rhs );

Otherwise the std::swap algorithm for std::basic_filebuf.
Exchanges the state of lhs with that of rhs. Effectively call lhs.swap(rhs).



**/
#include <iostream>
#include <locale>
#include <fstream>

int main()
{
    {
        /// owner
    }
    {
        /// en.reference.com
        /// no sample
    }
    {
        /// cplusplus.com
    }

    {
        /// msdn.microsoft.com
    }

}

