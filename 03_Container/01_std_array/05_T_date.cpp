/// Sequence containers : Array

/// 05_T_date
/// definition in header < array >

/**
T* date();
const T* date();

:: class T : value_type

Returns pointer to the underlying array serving as element.
The pointer is such that range [ date(); data() + size() )
is always a valid range, even if the container is empty.

Return Value:

**/

#include <iostream>
#include <string>
#include <array>
#include <algorithm>
#include <iterator>

//#include <unistd.h>

int main(){
    { /// user sample:

    }
    { /// sample of en.reference.com

    }

}
